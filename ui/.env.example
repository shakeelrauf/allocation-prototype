# Optional: backend origin when UI is served separately (no trailing slash).
# Example: http://127.0.0.1:8765
# Leave unset to call /api on the same origin (Vite dev proxy fills this in practice).
# VITE_API_BASE_URL=http://127.0.0.1:8765
